import 'package:flutter/material.dart';

class StudentSchedulePerClassPage extends StatelessWidget {
  const StudentSchedulePerClassPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("StudentSchedulePerClassPage"));
  }
}